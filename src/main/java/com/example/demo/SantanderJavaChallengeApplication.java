package com.example.demo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class SantanderJavaChallengeApplication {

	public static void main(String[] args) {
		SpringApplication.run(SantanderJavaChallengeApplication.class, args);
	}

}

// write comment about accessing test server on price controller
// write comment throughout code explaining thought process

// Hashmap map to continually update and provide the lastest price - so the price will always be the latest